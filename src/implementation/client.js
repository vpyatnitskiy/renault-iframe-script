import { resize, scroll, message, geolocate, getParentInfo, on, off } from './client-implementation'

export default {
    resize,
    scroll,
    message,
    geolocate,
    getParentInfo,
    on,
    off,
}
